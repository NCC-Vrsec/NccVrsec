var typed3 = new Typed('#typed3', {
    strings: ['VRSEC NCC'],
    typeSpeed: 90,
    backSpeed: 0,
    // backDelay: 500,
    // startDelay: 1000,
    smartBackspace: true, // this is a default
    loop: true,
    showCursor: false
});
